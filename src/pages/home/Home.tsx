import React, { FC } from "react";
import { HomeContainer, HomeTitle } from "./HomeStyle";

const Home: FC = () => {
  return (
    <HomeContainer>
      <HomeTitle>Welcome to the Homepage!</HomeTitle>
    </HomeContainer>
  );
};

export default Home;
